#include <stdio.h>
#include <stdlib.h>
int main(void)
{
    int a,i;
    printf('Enter number of rows:');
    scanf("%d",&a);

    for(i=1;i<=a;i++){
        printf();
    }
    system("pause");
    return 0;
}