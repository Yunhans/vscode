#include <stdio.h>                 			/* stdio.h 標頭檔 */
#include <stdlib.h>                 		/* stdlib.h 標頭檔 */
int main(void){                    			/* main 主函式 */
    int i = 5;								/* 宣告整數 */
    int j = 12;								/* 宣告整數 */
    printf("%d + %d = %d\n",i,j,i+j); 		/* i + j = i+j */
    system("pause");						/* 暫停讓我看看視窗 */
    return 0;								/* 結束 */
}
